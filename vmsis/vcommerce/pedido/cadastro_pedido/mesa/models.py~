# -*- coding: utf-8 -*-
from django.db import models

# Create your models here.
class Mesa(models.Model):
	class Meta:
		db_table = "mesa"
		verbose_name = "Mesa"
		verbose_name_plural = "Mesas"

	dsmesa = models.CharField(max_length=250,verbose_name="Descrição")

